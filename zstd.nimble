# Package

version = "0.1.0"
author = "fox0430"
description = "Nim bindings for zstd"
license = "MIT"
skipDirs = @["examples"]
skipFiles = @["README.md", "LICENSE"]
installDirs = @["zstd"]

# Dependencies

requires "nim >= 1.2.0"
